<div class="modal-header">
	<button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>


	<h3>Item Details</h3>
</div>
	<div class="modal-body">
		<div class="row-fluid">
		
			<p>Item Code: <strong>I{{item_details.product_code}}</strong></p>
			<p>Item : <strong>{{item_details.supply_name}}</strong></p>
			<p>Reorder Level:<strong> {{item_details.reorder_level}}</strong></p>
			<p>Target Level:<strong> {{item_details.target_level}}</strong></p>
			<p>Quantity Per Package:<strong> {{item_details.quantity_per_package}}</strong></p>
			<p>Quantity on Hand: <strong>I {{item_details.total_quantity}}</stron></p>
		
	<div class="span12">
	{{#if transactions}}
	<table id="supply-item-transaction" class="table table-striped table-bordered table-hover table-condensed data-table">
		<thead>
			<tr>
				<th class="center">#</th>
				<th>Type</th>
				<th class="hidden-480">Username</th>
				<th>Timestamp</th>
				<th>Quantity</th>
				<!-- <th class="hidden-480">Details</th> -->
				
			</tr>
		</thead>
		<tbody>
			{{#iter transactions}}
			<tr>
				<td class="center">{{@index}}</td>
				<td>{{name}}</td>
				<td class="hidden-480">{{personnel_name}}</td>
				<td>{{#format_date time "Y-m-d H:i:s"}}</td>
				<td>{{amount}}</td>
				<!-- <td class="hidden-480">{{details}}</td> -->
				
			</tr>
			{{/iter}}
		</tbody>
	</table>
	{{else}}
	<p>No transactions found.</p>
	{{/if}}
	</div>
</div>
</div>
<div class="modal-footer">
{{#if content_data}}
    <a type="button" href="{{controller_base_url}}supplier_export_excel_pull_transaction_sheet/{{content_data.transaction.id}}" class="btn btn-primary btn-small" ><i class="icon-save"></i> Export to Excel</a>
{{/if}}
	<button type="button" class="print-report btn btn-small btn-info" data-href="{{content_data.report_url}}" data-width="800" data-height="600">Print</button>
	<button type="button" data-dismiss="modal" class="btn btn-small">Close</button>
</div>
